# 2026-07-28

## repos 代码格式化
- 结构：`repos/frontend`（Vite+React TS/JS）、`repos/backend`（Spring Boot 3.3.2, Java 21, Maven）。
- 采用 Prettier 在 `repos` 根统一格式化：新增 `repos/package.json`（devDeps: prettier + prettier-plugin-java）、`repos/.prettierrc`、`repos/.prettierignore`。
- 配置：单引号、分号、printWidth=100、trailingComma=all、2 空格、LF 行尾；`.prettierignore` 排除 node_modules/dist/target/*.map/lock。
- 已格式化：后端 29 个 .java + application.yml；前端 main.tsx/styles.css/index.html/tsconfig.json/package.json。
- 跳过（Prettier 不支持）：`backend/pom.xml`(XML) 与 backend 的 .sql 文件，如需格式化需另接 spotless / sql-formatter。
- 注意：环境 java 为 1.8（后台要求 21），故 Java 用 node 端 prettier-plugin-java 而非 Maven 工具链。

# 2026-07-29

## UI 专项启动
- 用户要求对前端整体 UI 做全面升级，风格贴合"多语言 AI 售后诊断与客服平台"。
- 诊断现有 UI：功能完整但通用 SaaS 模板风，缺辨识度/层级/氛围；字体 Inter（AI 生成风）；诊断流程、AI↔Human、安全停止等核心隐喻未在视觉上表达；零动效。
- 创建 `docs/UI方案.md`（README 引用但此前缺失）作为设计系统基线：定位分析、风格定位（精密仪表控制台）、设计令牌（字体/色彩/圆角/阴影/动效）、组件库 v2、页面改版方向、实施路径 M0–M6、验收标准。
- 用户选择"先只出高保真原型"，暂不改动代码。
- 新建 `prototype/ui-prototype-2026.html`（新设计方向 Precision Instrument Console）：含 4 视图切换——设计令牌速览 / 登录 / 终端对话 / 管理后台(含 Flows 节点图差异化亮点)。字体 Sora+Manrope+JetBrains Mono(Google Fonts CDN，生产需改 @fontsource 自托管)；teal/orange/red 三元视觉；蓝图点阵底、诊断进度轨、AI/人工/安全三态消息、流式打字光标、KPI spark、SVG 流程节点图。
- 旧的 `prototype/ui-prototype.html` 为早期 Inter 风格原型，保留作历史对照。
- 下一步：用户评审原型后，按 M0–M6 落到真实前端代码（或先调整设计方向）。

## UI 专项 M0–M6 实施完成
- 用户批准方案并要求"开始实施 M0–M6，修改 UI 同时注意国际化"。
- 实际落地（命中的不仅是视觉，还顺带修复了此前损坏的构建）：
  - `tailwind.config.js` 全量改写：品牌色阶、ai/human/safety/ink/line/ok/warn/danger/info 令牌、fontFamily(sans=Manrope/display=Sora/mono=JetBrains Mono)、圆角/阴影、keyframes+animations（fade-in/slide-up/pop-in/pulse-ring/shimmer/spin）。
  - `index.css`：body 点阵画布底 `bg-canvas`、精修滚动条、`:focus-visible` 描边、utilities（.enter/.enter-up/.stagger/.shimmer/.glass）。
  - `main.tsx`：改用 `@fontsource-variable/{sora,manrope,jetbrains-mono}` 自托管字体（弃用 Google Fonts/Inter）。
  - `components/ui.tsx`：保留 Button/Input/Textarea/Tag/Modal/Spinner/EmptyState/StatusFlow，新增 Card(含 onClick)/Banner/Drawer/PageHeader/StatCard/Segmented/Table/Avatar/DiagnosisRail；可见文案全部由调用方传入（i18n 安全）。StatusFlow 简化状态机 draft→review→approved→published。
  - 新建 `lib/tenant.tsx`（原缺失，导致构建失败）：TenantProvider + useTenant（加载 /tenants、tenantId 持久化到 localStorage）。
  - `lib/types.ts` / `lib/api.ts`：补齐此前缺失的 ProductModel/ProductVariant/Firmware/Qr/KnowledgeDocument/Handoff/ModelProvider 类型与全套 CRUD 封装（list/create/revoke/searchV2/loadAnalytics 等），使各控制台页面可编译。
  - 重写/重排页面：`ConsoleLayout`(仪表控制台外壳：渐变侧栏+分组导航+活跃条、玻璃顶栏+健康点+租户/语言切换)、`SupportPage`(设备条+两栏+DiagnosisRail、AI/Human/Safety 三态消息+Banner+精排输入框)、`ProductsPage`/`QrsPage`/`DocumentsPage`/`SearchPage`/`HandoffsPage`/`ModelConfigsPage`/`FlowsPage`/`AnalyticsPage`/`Login` 全部接入新组件与数据层。
  - i18n：向 en.ts/zh.ts 补齐 console.groups、ui.*、products.*、common.all/processing、qrs.*、qrStatus.* 等键；修复误引用 common.actions→products.actions、qrs.region→common.region。
  - 修复的字段映射坑：`Product.displayName`(非 name)、`Product.firmwareMax`(非 latestFirmwareVersion)、`KnowledgeDocument.scope.productModelId`(非 productModelId)、`SearchHit.source` 与 vector/keyword 布尔。
- 验收：最终 `npx tsc --noEmit` 通过；`npm run check:i18n` 通过（en/zh 键全齐）；`npm run build` 通过（85 modules transformed，无 error/warning）。M0–M6 全量实施并验收完成。

# 2026-07-29

## 重写项目 README.md
- 依据实际代码全面重写 `README.md`：项目为「出海硬件厂商多语言 AI 售后诊断与客服平台」。
- 结构：项目简介与核心功能、技术架构与模块划分、环境依赖与快速启动、目录结构概览、核心业务流程（二维码绑定、知识运营、会话诊断编排、结构化诊断流程、人工转接）、相关文档导航。
- 关键事实：后端 `repos/backend`（Spring Boot 3.3.2 / Java 21，包 `com.hardwareai.support`，Flyway V1–V9）；前端 `repos/frontend`（React+TS+Vite+Tailwind+i18next）；基础设施 `infra/compose.yaml`（PostgreSQL18/MinIO/Qdrant/OCR PaddleOCR/Embedding BAAI-bge-m3+Rerank bge-reranker-v2-m3/api/web）。
- 重要修正：后端实际读取的环境变量为 `DATABASE_URL`（**非** `SPRING_DATASOURCE_URL`），正在运行的 `LOCAL_RUN.md` 用的是旧变量名，存在偏差——README 已按 `application.yml` 实际变量名记录。
- LLM 默认关闭（`LLM_ENABLED=false`），关闭时仍可运行意图分类/证据检索/诊断状态机/人工转接。
